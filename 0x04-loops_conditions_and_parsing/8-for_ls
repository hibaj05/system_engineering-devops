#!/usr/bin/env bash
# This script displays filenames, and if they contain '-', it strips everything before and including the first dash

for file in $(ls -1 | sort); do
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done

