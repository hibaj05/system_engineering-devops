#!/usr/bin/env bash
# This script lists filenames (excluding hidden ones), showing only the part after the first dash

for file in *; do
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
