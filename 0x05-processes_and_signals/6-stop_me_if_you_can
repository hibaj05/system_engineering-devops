#!/usr/bin/env bash
# This script stops the running '4-to_infinity_and_beyond' process
# using 'pkill', as 'kill' and 'killall' are forbidden.
# 'pkill -f' finds processes by matching the full command line and sends
# a termination signal (SIGTERM by default) to them.

pkill -f "4-to_infinity_and_beyond"
